# RStudio Docker Container

This Dockerfile creates a customizable RStudio container allowing users to work with R in a convenient environment.

## Docker Configuration

The Dockerfile installs R, RStudio, and necessary R packages, ensuring that users can start using R and RStudio as soon as the container is up and running. It also sets up the appropriate working directory and exposes the required ports for accessing RStudio in a browser.